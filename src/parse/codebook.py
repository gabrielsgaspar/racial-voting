"""Codebook parsing placeholders."""

from __future__ import annotations

from pathlib import Path


def parse_codebook(path: Path) -> dict[str, str]:
    """Return placeholder mapping from variable to label."""
    _ = path
    return {}
